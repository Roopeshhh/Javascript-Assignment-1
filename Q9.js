// 9.Declare a string variable text with any sentence.
let text1 = "Most cleanest animal in the world";

// • Find and print the length of the string.
console.log("The length of String is " + text1.length); //The length of String is 33

// • Access and print the first and last characters of the string.
console.log(text1[0]); //M
console.log(text1[text1.length - 1]); //d
