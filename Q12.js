// 12.Find and Replace a Word
// • Declare a string variable sentence with a sentence of your choice.
let str = "The example sentence";

// • Use the  method to find the position of a word in the sentence.
let srt1 = console.log(str.substring(4, 11)); //example

// • and Replace the word   print the updated sentence.
let str2 = console.log(str.replace(" ", " is ")); //The is example sentence
