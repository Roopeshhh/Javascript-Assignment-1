// 17.Declare a string variable strNumber with a value of "123.456".
let strNumber = "123.456";

// • Convert the string to a number using:

// • Number()
console.log(Number(strNumber));

// • parseInt()
console.log(Number.parseInt(strNumber));

// • parseFloat()
console.log(Number.parseFloat(strNumber));
// Print the results and compare the outputs.
