// 18.Declare a boolean variable isTrue with a value of true.

// Convert to a number and print the result
let numValue = Number(isTrue);
console.log(numValue); // Output: 1

// Convert to a string and print the result
let strValue = String(isTrue);
console.log(strValue); // Output: "true"
